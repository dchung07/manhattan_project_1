const Title = document.getElementById("title");  // Corrected the ID
const Start = document.getElementById("Start");  // Corrected the ID

Start.addEventListener("click", function() {
    window.location.href = 'question_page.html';
});